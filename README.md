# EmpManagementSys
Employee Management System is a project that manages employee details using collections and file handling. It supports CRUD operations—adding, viewing, updating, and deleting employee records—while ensuring data is saved and retrieved from files for future use.
